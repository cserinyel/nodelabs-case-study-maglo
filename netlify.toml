[build]
  command = "npm run build"
  publish = "dist"

# API Proxy - MUST come FIRST
[[redirects]]
  from = "/api/*"
  to = "https://case.nodelabs.dev/api/:splat"
  status = 200
  force = true

# SPA routing - catch-all comes LAST
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200